{
	"name": "fmpl",
	"version": "0.0.1",
	"description": "fucking template engine",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/tmont/fmpl.git"
	},

	"scripts": {
		"test": "node_modules/.bin/mocha -R dot --recursive tests",
		"coverage": "node_modules/.bin/nyc --include src --all --reporter=html --reporter=text node_modules/.bin/mocha -R dot --recursive tests"
	},

	"devDependencies": {
		"mocha": "3.2.0",
		"expect.js": "0.3.1",
		"nyc": "10.2.0"
	}
}
