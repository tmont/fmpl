{
	"name": "fmpl",
	"version": "1.2.0",
	"description": "fucking template engine",
	"keywords": [ "template", "engine" ],
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/tmont/fmpl.git"
	},

	"bin": {
		"fmpl": "./bin/fmpl.js"
	},

	"files": [
		"bin/fmpl.js",
		"index.js"
	],

	"scripts": {
		"test": "node_modules/.bin/mocha -R dot --recursive tests",
		"coverage": "node_modules/.bin/nyc --include index.js --all --reporter=html --reporter=text npm test"
	},

	"devDependencies": {
		"mocha": "3.2.0",
		"expect.js": "0.3.1",
		"nyc": "10.2.0"
	}
}
